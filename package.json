{
  "name": "nano-sglang-interactive-guide",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "check": "node scripts/check-content.mjs && node scripts/check-runtime.mjs && node scripts/check-labs.mjs && vitepress build docs"
  },
  "devDependencies": {
    "vitepress": "1.6.4",
    "vue": "3.5.13"
  },
  "engines": {
    "node": ">=20"
  }
}
